# Changelog

## 0.3.0

- Rebuilt the guided setup for first-time agent-loop users.
- Added a plain-English explanation of where the AI comes from.
- Replaced automatic agent discovery with an explicit user choice.
- Added permission before running the selected agent's version command.
- Removed the ambiguous current-folder default from the project prompt.
- Require a separate Git project and explain why.
- Added examples to every beginner prompt.
- Added explicit `CREATE` and `RUN` approval gates.
- Save run records inside the selected project's `.agent-loop/runs` folder by default.
- Rewrote the README as a complete start-to-finish walkthrough.

## 0.2.0

- Added the initial interactive `agent-loop start` workflow.

## 0.1.0

- Initial verifiable coding-agent loop.
